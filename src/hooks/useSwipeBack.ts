// Web stub — swipe-back is handled natively on iOS via NativeNav (now removed).
// Exported as no-op to avoid updating all call sites.
export const useSwipeBack = (_isEnabled: boolean, _onBack: () => void): void => {};
